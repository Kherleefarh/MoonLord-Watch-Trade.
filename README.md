# MoonLord-Watch-Trade

This is the MoonLord Watch Trade repository (clean initial commit).
It is prepared to push to GitHub. After extracting, run:

```bash
sh PUSH_TO_GITHUB.sh
```

The script will:
- Initialize Git
- Create branch main
- Add remote https://github.com/Kherleefarh/MoonLord-Watch-Trade.git
- Commit and push (you will authenticate locally)

Seeded admin credentials (change after deploy):
- Email: admin@moonlord.com
- Password: MoonLord@2025

See `backend/.env.example` and `frontend/README.md` for setup instructions.
update
